{
  "name": "typescript-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "colors": "^1.3.3"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "typedoc": "^0.14.2",
    "typescript": "^3.2.4"
  },
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && npx tsc",
    "prepare": "sh toMJS.bash",
    "start": "npm run build && npm run prepare && node --experimental-modules ./dist/index.mjs --no-warnings",
    "test": "npx jest",
    "docs": "npx typedoc --out docs/ src/ --exclude src/**/*test.ts"
  },
  "author": "",
  "license": "ISC"
}
